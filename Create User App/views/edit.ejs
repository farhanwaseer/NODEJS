<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create User App</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full h-full  bg-zinc-800">
        <nav class="p-4 bg-red-800">
            <h3 class="text-zinc-300 text-left ">Create User App</h3>
            <ul class="text-zinc-300 px-2  text-right">
                <a class="p-2 " href="/">Home</a>
                <a class="p-2 " href="/read">Read</a>
               
            </ul>
        </nav>
        <h1 class="text-3xl tracking-tighter  text-white p-5">Update User </h1>
        <form class=" p-5 text-white  " action="/update/<%= user._id %>" method="post">
            <input value="<%= user.name %>" required class="block px-5 border-2 border-zinc-600 rounded-lg py-2  bg-transparent " type="text"  placeholder="Name" name="name" >
            <input value="<%= user.email %>" required class="block px-5 border-2 border-zinc-600 rounded-lg py-2 mt-2 bg-transparent " type="email" placeholder="Email" name="email">
            <input value="<%= user.image %>" required class="block px-5 border-2 border-zinc-600 rounded-lg py-2 mt-2 bg-transparent " type="text"  placeholder="ImgUrl" name="image" >
            <input type="submit" class="mt-2 rounded-lg px-5 py-2 bg-yellow-500 "  value="Update User">
        </form>
    </div>
</body>
</html>