<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create User App</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="main w-full h-full  bg-zinc-800">
    <nav class="p-4 bg-red-800">
        <h3 class="text-zinc-300 text-left ">Create User App</h3>
        <ul class="text-zinc-300 px-2  text-right">
            <a class="p-2 " href="/">Home</a>
            <a class="p-2 " href="/read">Read</a>            
        </ul>
    </nav>
    <h1 class="text-3xl tracking-tighter mb-3 text-white p-10">All Users </h1>
 <div class="px-4 flex gap-3 flex-wrap ">

     <!-- Profile Card -->
      <% if(readUser.length > 0) { %>
        <% readUser.forEach(user => { %>
            <div class="users ">
                <div class="user w-full  p-4 bg-zinc-700 rounded-lg">
                    <img class=" max-w-72 min-w-72 h-72 rounded-lg bg-red-500 " src='<%= user.image %>' alt="">
                    <!-- <div class="w-full h-52 rounded-lg bg-red-500 ">
                        <img src="" alt="">
                    </div> -->
                    <h3 class="text-zinc-200 text-2xl tracking-tighter mt-3 "><%= user.name %></h3>
                    <h5 class="text-zinc-400 mt-2"><%= user.email %></h5>
                     <!-- Button Dropdown -->
                    <div class="mt-4 dropdown  text-right">
                        <button style="font-size:20px;color:rgb(146, 139, 139);padding: 0;" class="btn p-2" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">   
                          <i class="fa fa-ellipsis-v p-2 "></i>  
                        </button>
                        <ul  class="dropdown-menu bg-zinc-800 px-3 py-2" aria-labelledby="dropdownMenuButton">
                          <li><a class="text-zinc-400 " href="/edit/<%= user._id %>">edit-user</a></li>
                          <li><a class="text-zinc-400" href="/delete/<%= user._id %>">delete </a></li>
                        </ul>
                    </div>
                     <!-- Button Dropdown End-->
                </div>
            </div>             
        <%  }); %>

        <% } else { %>
            <h3>No user Yet</h3>
       <% } %>      
 </div>
</body>
</html>